#include "iterator.h"
template<typename h>
iterator<i>::iterator()
{
}

template<typename h>
iterator<h>::iterator(number<i> object)
{
	current = object;
}

template<typename h>
iterator<h> iterator<h>::operator++()
{
	
}
